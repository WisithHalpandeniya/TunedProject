##tuned global project 1 starting up on node.js
##Use Gitbash if on Windows
##Install necessary packages for Tuned driver and the server by going in to the two directories and running: 
-npm install
-npm install -g nodemon
-node app.js

##Credentials (api tokens and id ) are provided in curlrequest.sh and their respective locations
## as well as further clarification on UI and server side access.